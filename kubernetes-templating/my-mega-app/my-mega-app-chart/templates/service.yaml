apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-mega-app.fullname" . }}
  labels:
    {{- include "my-mega-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-mega-app.selectorLabels" . | nindent 4 }}
